/// <reference types="cypress" />

// it.only runs only that test
// it.only('Test one', () => {
//     cy.visit('localhost:3000');
// })

// beforeEach() is a hook that runs that block before each test
// beforeEach(() => {
//     cy.visit('localhost:3000');
// });

describe('Group 1', () => {
    it('Test one', () => {
        cy.visit('localhost:3000');
    })
        
    it('Test two', () => {
            
    })}
);

describe('Group 2', () => {
    it('Test one', () => {
        cy.visit('localhost:3000');
    })
        
    it('Test two', () => {
            
    })}
);